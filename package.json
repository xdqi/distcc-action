{
  "name": "distcc-action",
  "version": "1.0.0",
  "private": true,
  "description": "Node 24 wrapper that resolves and runs the prebuilt distcc-action Go binary",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/io": "^1.1.3",
    "@actions/tool-cache": "^2.0.2",
    "semver": "^7.6.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
